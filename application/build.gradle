plugins {
  id 'java-conventions'
  id 'java-library'
}

group 'io.aerodata.core.application'


dependencies {
  implementation(project(":domain"))
  testImplementation(project(path: ":domain", configuration: "testClases"))
}